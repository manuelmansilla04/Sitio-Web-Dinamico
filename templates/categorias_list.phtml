<?php require_once 'layout/header.phtml' ?>

<section class="show_categorias container my-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="fw-bold text-dark">Categorias</h2>
        <?php if ($user): ?>
            <a href="addCategoria" class="btn btn-primary btn-sm">+ Nueva Categoria</a>
        <?php endif; ?>
    </div>

    <div class="row g-4">
        <?php foreach($categorias as $s): ?>
            <div class="col-sm-6 col-md-4 col-lg-3">
                <div class="card shadow-sm border-0 h-100">
                    <a href="list_categorias/<?= $s->id_categoria ?>" class="text-decoration-none text-dark">
                        <div class="card-body text-center">
                            <h5 class="card-title fw-semibold mb-2"><?= htmlspecialchars($s->nombre) ?></h5>
                        </div>
                    </a>
                    <?php if ($user): ?>
                        <div class="card-footer bg-transparent border-0 text-center">
                            <a href="editCategoria/<?=$s->id_categoria?>" class="btn btn-warning btn-sm me-2">Editar</a>
                            <a href="deleteCategoria/<?=$s->id_categoria?>" class="btn btn-danger btn-sm">Eliminar</a>
                        </div>
                    <?php endif; ?>
                </div>
            </div>
        <?php endforeach; ?>
    </div>

    <img src="images/DifusoresRegistro.jpg" alt="Image">

</section>

<?php require_once 'layout/footer.phtml' ?>